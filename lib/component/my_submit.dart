import 'package:flutter/material.dart';

class MySubmit extends StatelessWidget {
  const MySubmit({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Padding(padding: EdgeInsetsGeometry.symmetric(horizontal: 25)),
      ),
    );
  }
}
